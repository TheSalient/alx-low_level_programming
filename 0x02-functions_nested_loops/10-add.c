#include "main.h"
/**
 * add- This is a function islower.
 * @c : This is the parameter.
 * @b : This is the parameter.
 * Return: Always 0 and 1 for true and false.
 */
int add(int c, int b)
{
int d = 0;
d = c + b;
return (d);
}
