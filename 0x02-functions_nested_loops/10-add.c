#include "main.h"
/**
 * _abs- This is a function islower.
 * @c, @b: This is the parameter.
 * Return: Always 0 and 1 for true and false.
 */
int add(int c, int b)
{
int d = 0;
d = c + b;
return (d);
}