#include "main.h"
/**
 * _abs- This is a function islower.
 * @c: This is the parameter.
 * Return: Always 0 and 1 for true and false.
 */
int _abs(int c)
{
if (c >= 0)
{
return (c);
}
else
{
c = c * -1;
return (c);
}
}
