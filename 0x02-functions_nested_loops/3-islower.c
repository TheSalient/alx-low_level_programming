#include "main.h"
/**
 * _islower- This is a function islower.
 * @c: This is the parameter.
 * Return: Always 0 and 1 for true and false.
 */
int _islower(int c)
{
if (c >= 97 && c < 123)
{
return (1);
}
else
{
return (0);
}
}
