#include "main.h"
#include <stdio.h>
/**
* main - function
*
* @argc: the chaine
*@argv: another parameter
* Return: Always 0.
*/
int main(int argc, char **argv)
{
int i;
for (i = 0; i < argc; i++)
{
Printf("%s\n", argv[i]);
return (0);
}
