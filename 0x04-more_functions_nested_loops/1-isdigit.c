#include "main.h"
/**
 * _isdigit - This is a function
 * @c: This is the parameter
 * Return: Always l and 0
 */
int _isdigit(int c)
{
int n = c;
if (n >= 0 && n <= 9)
return (1);
else
{
return (0);
}
}
