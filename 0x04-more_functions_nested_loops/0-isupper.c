#include "main.h"
/**
 * _isupper - This is a function
 * @c: This is the parameter
 * Return: Always l and n
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
